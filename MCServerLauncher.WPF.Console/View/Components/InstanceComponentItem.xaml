<UserControl x:Class="MCServerLauncher.WPF.Console.View.Components.InstanceComponentItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="800">
    <Grid Height="36">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="ComponentFileNameTextBlock" Margin="0,0,15,0" Grid.Column="0"
                   Style="{StaticResource MinititleTextBlock}" VerticalAlignment="Center" />
        <Button Grid.Column="1" Height="30" Margin="0,0,0,0">
            <Button.Content>
                <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.More}" FontSize="16" />
            </Button.Content>
        </Button>
    </Grid>
</UserControl>