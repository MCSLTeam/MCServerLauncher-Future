<UserControl x:Class="MCServerLauncher.WPF.View.Components.ResDownloadItem.FastMirrorResCoreItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="165">
    <Border Style="{StaticResource StyledBorder}" Width="170" Height="40" x:Name="CoreItemBorder" Margin="0,-5,0,-5">
        <Grid Width="Auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Margin="10,0,5,0" TextAlignment="Center" VerticalAlignment="Center" Grid.Column="0"
                       x:Name="CoreNameReplacer" />
            <Button IsHitTestVisible="False" Padding="5,2,5,2" x:Name="CoreTagView" Height="25" Margin="5,0,10,0" VerticalAlignment="Center" Style="{StaticResource AccentButtonStyle}"
                    Grid.Column="1">
            </Button>
        </Grid>
    </Border>
</UserControl>